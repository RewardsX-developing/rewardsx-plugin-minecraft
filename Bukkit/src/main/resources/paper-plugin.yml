name: RewardsX
version: '${project.version}'
api-version: '1.19'
description: RewardsX Minecraft plugin for Paper
authors:
  - R_Developing

main: net.r_developing.rewardsx.Plugin
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true